services:
  web:
    build: .
    ports:
      - "8082:8080"
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - RUST_LOG=info
      - INITIAL_DELAY_HOURS=${INITIAL_DELAY_HOURS:-0}
    volumes:
      - ./data:/app/data
